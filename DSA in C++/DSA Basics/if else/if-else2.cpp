#include<iostream>
using namespace std;

int main(){
    int a = 24;
    if (a>20){
        cout<<"Fedora"; // fedora is worth, still using it
    }
    else if(a==24){
        cout<<"Arch"; // your time your arch
    }
    else{
        cout<<"Ubuntu"; // Used when I was novice, but is now dusty. 
    }

    return 0;
}